#include <bits/stdc++.h>

using namespace std;

vector<vector<int>> mergedOverLappingIntervals(< vector<vector<int>> & arr)
{
    int n = arr.size();

    sort(arr.begin(), arr.end());

    vector<vector<int>> ans;

    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j <)
    }
}